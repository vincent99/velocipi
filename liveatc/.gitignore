# Build output
/intercom-stt
/dist/
/tmp/
/build-errors.log

# Node (root package.json: concurrently for `yarn dev`)
/node_modules/

# Local config overrides (defaults live in config.default.yaml)
/config.yaml

# Recorded data / transcripts
/data/

# Python sidecar venv + caches
.venv
__pycache__

# Editor / OS cruft
*~
*.swp
.DS_Store
